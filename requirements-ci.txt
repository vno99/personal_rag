# Mirror of requirements.txt stripped of the torch / torchvision /
# torchaudio lines. The CI workflow regenerates this file on every
# run with `grep -v`, so there is no manual drift to track: see
# .github/workflows/ci.yml.
#
# Do not edit by hand. The file is committed only so that local
# `pip install -r requirements-ci.txt` works without the grep step
# (e.g. on a developer machine that wants the same env as CI).
langchain-community==0.4.1
lxml==6.1.0
beautifulsoup4==4.14.3
tqdm==4.67.3
transformers==5.6.2
weaviate-client==4.21.0
langchain_huggingface==1.2.2
sentence-transformers==5.4.1
